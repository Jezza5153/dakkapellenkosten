import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    // The existing static HTML files (index.html, blog.html, etc.) live in public/
    // and are served automatically by Next.js
};

export default nextConfig;
